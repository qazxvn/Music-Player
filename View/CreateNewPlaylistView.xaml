<Window x:Class="MusicPlayer.View.CreateNewPlaylistView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:MusicPlayer.View"
        mc:Ignorable="d"
        Title="CreateNewPlaylist" Height="150" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Название плейлиста" HorizontalAlignment="Center"></TextBlock>
        <TextBox Grid.Row="1" Text="{Binding PlaylistName}" Width="115" ></TextBox>
        <Button Grid.Row="2" Width="115" Margin=" 0 10 0 0" Command="{Binding CreatePlaylistCommand}">Создать плейлист</Button>
    </Grid>
</Window>
